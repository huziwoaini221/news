# 项目方案：个人工具中枢

> **网页负责操作，Cloudflare Worker 负责后端，D1 负责数据，MyPlace 负责定时唤醒，企业微信只负责通知。**

## 核心原则

1. 不把所有东西做成一个大系统。
2. Cloudflare Worker 作为中枢（API + 通知 + 晨报）。
3. 现有工具继续独立运行，课时 / VaultLite / 笔记一律不迁移。
4. 企业微信**只做通知中心**，不做交互入口。
5. MyPlace PHP 负责可靠地"叫醒"定时任务，不依赖 Cloudflare Cron。

---

## 一、最终架构

```text
                  你
                  │
        ┌─────────┼──────────┐
        │         │          │
        ▼         ▼          ▼
     任务网页    日历网页    财务网页    通讯录网页
        │         │          │
        └─────────┼──────────┘
                  ▼
          Cloudflare Worker
                  │
         ┌────────┴────────┐
         ▼                 ▼
         D1           Notification Service
                          │
                          ▼
                     企业微信通知
```

现有应用：

```text
课时记录 ──────────→ 企业微信通知（恢复通知链路）
VaultLite ─────────→ 仅导航入口，不通知
笔记     ─────────→ 隐藏入口，不通知、不读取
```

---

## 二、全局规则（必须写死）

1. **时区**：数据库全部存 UTC；所有业务计算统一使用 `Asia/Shanghai`（UTC+8）。"今天"= 北京时间 00:00 ~ 次日 00:00，先换算成 UTC 再查询。
2. **交互方式**：所有业务在自己的 Web UI 里操作。企业微信不解析自然语言、不做机器人 CRUD、不做菜单表单。
3. **通知统一**：所有模块的通知都走 Notification Service，不各自直连企业微信。
4. **定时来源**：MyPlace PHP Cron 为主触发器，Cloudflare Cron 仅作备用。
5. **幂等**：代码判断 + 数据库 UNIQUE 双保险。
6. **Webhook 隔离**：企业微信 Webhook URL 只存在 Worker 端 Secret，外部应用只能调用通知 API。
7. **数据互不读取**：晨报第一版只汇总 Worker 自己管理的模块，不读取课时数据。

---

## 三、模块清单

| 模块     | 是否新做 | 交互入口       | 企业微信通知 |
| -------- | ---- | ---------- | ------ |
| 📋 任务   | 新做 | Web UI     | ✅      |
| 📅 日历   | 新做 | Web UI     | ✅      |
| 💰 财务   | 新做 | Web UI     | ✅      |
| 👤 通讯录  | 新做 | Web UI     | ✅      |
| 📚 课时   | 已有 | 独立 Web UI  | ✅（恢复）  |
| 🔐 VaultLite | 已有 | 独立 Web UI  | ❌ 仅导航   |
| 📝 笔记   | 已有 | 独立 Web UI  | ❌ 隐藏入口  |

**课时、VaultLite、笔记全部不迁移、不重做。**

---

## 四、四个新模块（Web UI + API + D1 + 通知）

### 任务

- 网页：新建 / 修改 / 删除 / 完成 / 延期 / 设置提醒 / 今天任务 / 明天任务 / 未完成任务
- 数据：

```text
tasks
├── id
├── title
├── description
├── due_at            # UTC
├── reminder_at       # UTC
├── status            # pending / completed / cancelled
├── created_at
└── updated_at
```

- 操作成功后通知企业微信。

### 日历

- 网页：添加 / 修改 / 删除日程、查看今天 / 本周
- 独立于任务，不自动生成 Task。
- 数据：

```text
calendar_events
├── id
├── title
├── start_at          # UTC
├── end_at            # UTC
├── location
├── description
├── reminder_at       # UTC
└── status
```

### 财务

- 网页：收入 / 支出 / 分类 / 修改 / 删除 / 今日 / 本月 / 本年度 / 分类统计
- 数据：

```text
transactions
├── id
├── type              # income / expense
├── amount
├── category
├── description
├── occurred_at       # UTC
├── created_at
└── updated_at
```

- 保存后通知，例如：

> 💰 记账成功
> 支出 ¥35
> 餐饮｜午餐

### 通讯录

- 网页：新增 / 编辑 / 删除 / 搜索
- 数据：

```text
contacts
├── id
├── name
├── phone
├── email
├── company
├── position
├── note
├── created_at
└── updated_at
```

- 通知策略：新增/修改通知，查询不通知。

---

## 五、数据边界

Worker D1 只保存：

```text
tasks
calendar_events
contacts
transactions
notification_logs
morning_reports
```

课时 / VaultLite / 笔记各自使用自己的数据系统。

---

## 六、Notification Service

统一通知，避免"Webhook 换了要改四个项目"。

```text
              Notification Service
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
     realtime     reminder     report
        │           │           │
        ▼           ▼           ▼
    即时通知      到期提醒      每日晨报
        │           │           │
        └───────────┼───────────┘
                    ▼
                企业微信
```

### 通知日志（必做）

```text
notification_logs
├── id
├── source          # tasks / calendar / finances / lessons
├── event_type
├── payload
├── message
├── status
├── retry_count
├── error
├── created_at
└── sent_at
```

出问题时能明确判断：事件有没有发出 → Worker 有没有收到 → 有没有调用企业微信 → 返回了什么。

---

## 七、定时系统

### 主触发器：MyPlace PHP Cron

```text
MyPlace PHP Cron
        │
        ├── 每 5 分钟 → POST /api/check-reminders
        │
        └── 每日 08:00 → POST /api/morning-report
```

- `/api/check-reminders`：查询 `reminder_at <= now AND status = pending`，发送到期提醒。
- 提醒精度约 **0～5 分钟**，个人系统可接受。
- Cloudflare Cron 保留作备用，幂等保证不会重复发送。

### 晨报

```text
MyPlace Cron → Worker /api/morning-report
        │
        ├── 今日任务
        ├── 今日安排
        └── 财务摘要（昨日）
        │
        ▼
    企业微信
```

- 第一版**不读取课时数据**（单向通知可以，数据依赖不做）。
- 幂等：`morning_reports` 表对 `report_date` 加 UNIQUE 约束，代码 + 数据库双保险，多触发器同时执行也不会发两份。

```sql
CREATE TABLE morning_reports (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    report_date TEXT NOT NULL,
    status TEXT NOT NULL,
    sent_at TEXT,
    created_at TEXT NOT NULL,
    UNIQUE(report_date)
);
```

---

## 八、企业微信

只作为通知中心，例如：

> 📋 任务已添加
> 明天 09:00
> 交材料

> 🔔 任务提醒
> 现在该交材料了

> 📚 课时记录
> 英语
> 1小时

- Webhook URL 只存在 Worker Secret（`WECOM_WEBHOOK_URL`）。
- 课时项目只持有 `NOTIFICATION_API` + `API_SECRET`，不知道 Webhook。

### 个人工具导航

```text
🏠 个人工具
📋 任务
📅 日历
👤 通讯录
💰 财务
📚 课时
🔐 VaultLite
```

笔记入口隐藏（保留，不删除）。

---

## 九、课时通知恢复（独立项目，重点）

**不重做、不迁移、不修改现有核心业务逻辑。**

只做一件事：恢复之前存在、后来因多次修改而消失的企业微信通知。

```text
课时系统 → 业务事件 → Notification API → Worker → 企业微信
```

事件类型（以现有功能为准）：

```text
lesson_created
lesson_updated
lesson_deleted
lesson_completed
```

验证标准：

```text
新增课时 → 数据库成功 → 企业微信收到通知（连续测试多次）
```

---

## 十、安全

- Worker API：所有外部调用带 `Authorization: Bearer <API_SECRET>`。
- 课时系统使用独立 Secret，任何外部应用不能调用通知接口。
- `/api/morning-report` 同样需要 Secret。
- 企业微信 Webhook 不外泄，只存在于 Worker 端。

---

## 十一、D1 备份

数据量小，备份成本极低。

- **每日**：`wrangler d1 export` 导出核心数据。
- **每周**：保留一个完整快照。

```text
backup/
├── 2026-08-15.sql
├── 2026-08-16.sql
└── weekly/
    └── 2026-W33.sql
```

备份落到本地或 R2。

---

## 十二、Cloudflare 项目结构

```text
personal-hub/
│
├── src/
│   ├── index.js
│   │
│   ├── routes/
│   │   ├── tasks.js
│   │   ├── calendar.js
│   │   ├── contacts.js
│   │   ├── finances.js
│   │   ├── notification.js
│   │   ├── check-reminders.js
│   │   └── morning-report.js
│   │
│   ├── services/
│   │   ├── wecom.js
│   │   ├── notification.js
│   │   ├── reminder.js
│   │   └── morning.js
│   │
│   └── db/
│       ├── tasks.js
│       ├── calendar.js
│       ├── contacts.js
│       └── finances.js
│
├── web/              # 四个模块的 Web UI 静态文件
│
├── migrations/
│
├── wrangler.jsonc
│
└── package.json
```

---

## 十三、开发顺序

### Phase 1：通知基础设施
- Worker → 企业微信：测试消息验证
- `notification_logs`

### Phase 2：恢复课时通知（优先级最高）
- 找到原课时通知失效原因 → 修复 → 多次连续测试

### Phase 3：任务
- `tasks` 表 + API + Web UI + 通知

### Phase 4：日历
- `calendar_events` 表 + API + Web UI + 通知

### Phase 5：财务
- `transactions` 表 + API + Web UI + 通知

### Phase 6：通讯录
- `contacts` 表 + API + Web UI + 通知

### Phase 7：晨报 + 定时
- `/api/morning-report` 手动测试 → MyPlace Cron
- `/api/check-reminders` + MyPlace 每 5 分钟
- `morning_reports` UNIQUE 幂等

### Phase 8：导航
- 个人工具入口（任务/日历/通讯录/财务/课时/VaultLite）
- 笔记隐藏

---

## 十四、系统边界

| 组件           | 职责 |
| -------------- | ---- |
| Cloudflare Worker | 任务 + 日历 + 财务 + 通讯录 + 统一通知 + 提醒 + 晨报 |
| 课时记录       | 自己的课时业务，只接入通知事件 |
| VaultLite      | 自己的密码业务，只提供导航入口 |
| 笔记           | 自己的笔记业务，隐藏入口，完全隔离 |
| MyPlace        | 每天/每 5 分钟定时叫醒接口，不保存数据、不做业务 |
| 企业微信       | 只接收通知 + 提供导航入口 |

最大的价值：**已经做好的东西基本不动，新系统也不会把它们绑死在一起。**尤其是课时通知，作为单独的"故障恢复 + 通知接口"项目处理，以后调整课时 UI 和业务逻辑，不会轻易丢掉通知链路。
